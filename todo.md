# Project TODO

- [x] 建立倉儲站點作業與 KPI 管理系統的 MVP 資料模型與路由骨架
- [x] 實作登入後角色導向：工程師進入站點作業總覽，管理者／主管進入 KPI 儀表板
- [x] 實作站點作業總覽頁，顯示 A1、A2、B、C、D、E、待入庫卡片與未完成數量、今日新增、逾期件數
- [x] 實作 A1 點到貨作業頁，支援掃碼輸入、商品資料卡、推進下一站、返回站點總覽
- [x] 實作 A2 安裝作業頁，支援掃碼輸入、商品資料卡、推進下一站、返回站點總覽
- [x] 實作 B 站軟測作業頁，支援掃碼輸入、商品資料卡、推進下一站、返回站點總覽
- [x] 實作 C 站品檢作業頁，支援掃碼輸入、商品資料卡、推進下一站、返回站點總覽
- [x] 實作 E 站抹除作業頁，支援掃碼輸入、商品資料卡、推進下一站、返回站點總覽
- [x] 實作 D 站抽樣頁，顯示抽樣清單與結果，抽樣不通過時返工回 C 站重新檢測
- [x] 建立指定資料表 products、station_tasks、station_events、productivity_target_configs 與相關輔助表
- [x] 實作所有站點作業先寫入 DB 的主流程
- [x] 實作 Google Sheet 非同步回寫機制，確保不阻塞主流程
- [x] 實作工程師工作點數 KPI 基礎版，計算當日總點數、原始達成率、KPI 達標率、超標率
- [x] 實作四大 KPI 維度摘要：產能達成、品質表現、時效表現、出勤公平性
- [x] 實作工程師績效頁，顯示每日點數明細與月累積統計
- [x] 實作管理者後台：帳號管理、站點規則設定、品類／子分類設定、標準產能設定
- [x] 實作六個月資料歸檔機制，主表僅保留近期資料
- [x] 套用極簡北歐風格視覺設計，完成前端主題與版型
- [x] 撰寫與更新 Vitest 測試，覆蓋 MVP 關鍵流程
- [x] 完成 MVP 驗證、狀態檢查與最終交付
- [x] 修復首頁載入時 station_rules 初始化寫入失敗造成的 API Query Error，並驗證首頁可正常開啟
- [x] 修復 /admin 頁面載入時 stationCode 驗證失敗造成的 API Query Error，並驗證管理頁可正常顯示
- [x] 對照使用者提供的回收品檢系統 ERD，重新檢查目前資料表與頁面是否一致
- [x] 在管理後台新增可直接編輯站點規則的表單
- [x] 在管理後台新增可直接編輯標準產能設定的表單
- [x] 修正站點總覽入口與 A1、A2、B、C、E 作業頁的可見性與導覽問題
- [x] 重新驗證首頁、管理頁與站點作業頁是否都能正常顯示
- [x] 修復 /station/:stationCode 頁面載入時 stationCode 驗證失敗造成的 API Query Error，並驗證站點頁可正常顯示
- [x] 新增匯入作業入口與對應頁面，讓管理者可匯入站點作業所需商品資料
- [x] 在 A1 點到貨頁補上商品批號、商品序號、IMEI（選填）與品名輸入／顯示欄位
- [x] 在 B 站軟測頁新增故障狀態選單與可維護的功能表設定
- [x] 在 C 站品檢頁新增故障項目與外觀項目選單與可維護的功能表設定
- [x] 重新驗證匯入作業、A1、B、C 頁面流程是否符合回收品檢現場作業需求
- [x] 提供匯入作業可下載的 CSV 範本檔
- [x] 將匯入作業頁改為支援 CSV 檔案上傳按鈕，不再只依賴貼上內容
- [x] 將匯入頁與 A1 點到貨頁的品名欄位改為下拉式選單
- [x] 在管理後台新增品名管理功能，支援新增與刪除
- [x] 調整匯入規則：廠商與商品分類必填，商品批號／商品序號／IMEI 任一有值即可匯入，商品批號／商品序號／IMEI／品名可留空
- [x] 在本地資料庫新增到貨時間欄位，區分匯入時間與實際到貨時間
- [x] 建立每日回寫指定 Google Sheet「採購單」工作表的同步流程
- [x] 調整每日 Google Sheet 同步邏輯：依採購單既有列補齊缺漏欄位，不覆蓋完整工作表
- [x] 調整匯入介面與資料模型：同一次匯入共用一個到貨時間
- [x] 為採購單同步腳本補上 Vitest 單元測試，驗證既有列比對、僅補空白欄位、不覆蓋既有非空值與新增列行為
- [x] 將每日採購單同步排程的建立方式文件化或納入可重建的專案設定
- [x] 補充採購單同步的可重建驗證路徑，證明工作表補寫規則正確
- [x] 提供符合現行匯入規則的匯入作業範例 CSV
- [x] 將匯入作業範例 CSV 掛到匯入頁的「下載範例」按鈕
- [x] 將匯入作業相關 CSV 範例與說明中的標頭改為中文
- [x] 將匯入頁說明文案中的 CSV 標頭示例統一改為全中文
- [x] 補上回歸測試，驗證匯入頁說明文字顯示中文 CSV 標頭
- [x] 修正匯入作業下載 CSV 的中文編碼，避免 Excel 開啟顯示亂碼
- [x] 修正範例 CSV 中 IMEI 在 Excel 開啟時被轉為科學記號的問題
- [x] 在 A1 站頁顯示目前待點貨的商品分類與數量摘要，讓匯入完成後可直接查看待處理分布
- [x] 從使用者提供的 Excel 商品編碼檔擷取品名，並新增到品名管理
- [x] 刪除現有所有品名管理資料，改以使用者新提供的 0.商品編碼(1).xlsx 重新匯入品名
- [x] 調整匯入頁下方區塊 UI：顯示已匯入未完成點貨的採購單號、商品類別與總數量
- [x] 修正匯入資料未自動生成採購單號的問題
- [x] 設計採購單列表可點擊展開細項的互動與視覺呈現
- [x] 在實際系統修正匯入資料未自動生成採購單號的問題
- [x] 在匯入頁顯示已匯入但未完成 A1 點貨的採購單摘要（採購單號／商品類別／總數量）
- [x] 讓匯入頁中的採購單摘要支援點擊展開品項細項
- [x] 驗證未手動填寫 poNumber 的匯入流程，確認 products.poNumber 會自動生成
- [x] 驗證既有待 A1 資料缺漏 poNumber 時會被自動回補
- [x] 驗證匯入頁會正確顯示待 A1 點貨的採購單摘要欄位與資料
- [x] 驗證點擊採購單摘要列後可展開並顯示品項細項
- [x] 修復匯入頁點擊匯入後沒有反應的 bug
- [x] 驗證匯入請求有正確送出並回傳成功或錯誤提示
- [x] 補上匯入無反應問題的回歸測試
- [x] 刪除目前待點貨摘要中顯示的 5 筆錯誤採購單資料
- [x] 修正同一次匯入被拆成多張 PO 單號的問題，確保整批資料共用同一張 PO
- [x] 修正待點貨採購單摘要總數量只顯示 1 的問題，讓其正確反映整批匯入筆數（例如 370）
- [x] 驗證刪除錯誤採購單後，待點貨摘要不再顯示這 5 筆資料
- [x] 驗證 370 筆匯入情境會顯示為單一 PO，且總數量正確
- [x] 修復匯入時反覆出現「網頁無回應」對話框的問題
- [x] 釐清匯入流程是否因前端同步處理過久而阻塞主執行緒
- [x] 以大批量 CSV 實際重跑一次瀏覽器匯入並保存驗證結果
- [x] 驗證大批量匯入時頁面不再跳出無回應提示
- [x] 修正匯入頁未從 CSV 正確讀取廠商欄位的問題
- [x] 驗證含廠商欄位的 CSV 載入後會自動帶入共用廠商欄位
- [x] 驗證使用含廠商欄位的 CSV 按下開始匯入時，不再要求額外手動輸入廠商
- [x] 修正 CSV 明明含資料卻被判定為「目前沒有可匯入的資料」的問題
- [x] 將匯入頁的 CSV 檔案上傳區塊調整到頁面最上方
- [x] 驗證上傳含資料的 CSV 後可正常辨識可匯入列並允許開始匯入
- [x] 釐清匯入頁廠商自動帶入為何顯示為綠途未來股份有限公司，而非使用者實際 CSV 的廠商值（例如悠優）
- [x] 修正匯入頁廠商欄位帶入邏輯，確保以上傳 CSV 的實際廠商內容為準，不受測試資料或前次頁面狀態影響
- [x] 驗證上傳廠商為悠優的 CSV 後，頁面共用廠商欄位與後續匯入皆顯示悠優
- [x] 釐清為何 `tmp/import-large-youyou-validation.csv` 共 370 筆資料，實際匯入後僅建立 `PO-20260422-16` 的 135 筆待點貨資料
- [x] 檢查大批量匯入時前端實際送出的 rows 數量是否被截斷，並確認是否與頁面只預覽前 30 筆的機制有關
- [x] 修正大批量 CSV 匯入被截斷的問題，確保 370 筆資料會完整建立於同一張 PO
- [x] 補上大批量匯入筆數一致性的回歸測試
- [x] 重新驗證 370 筆且廠商為悠優的 CSV 實際匯入後，PO 筆數為 370 且廠商資訊正確
- [x] 刪除指定匯入採購單 `PO-20260422-19`、`PO-20260422-16`、`PO-20260422-15` 的 products、station_tasks，以及已核對為 0 筆的 station_events、sampling_results、productivity_score_details
- [x] 核對三張指定 PO 在刪除前的商品筆數與關聯任務筆數
- [x] 驗證三張指定 PO 刪除後，不再出現在匯入頁待點貨採購單摘要，且 DB 查詢確認 A1 待點貨任務已無殘留
- [x] 釐清使用者提供的 CSV `import-products-example_756ddafb(1).csv` 為何在按下開始匯入後仍被判定為「目前沒有可匯入的資料」
- [x] 檢查匯入頁對 `智慧手機` 類別名稱、商品序號與 IMEI 三擇一條件的前端判定是否符合規則
- [x] 修正匯入頁 CSV 載入後的可匯入列判定邏輯，確保 `廠商 + 商品分類 + 商品批號/商品序號/IMEI 任一` 的資料可正常送出
- [x] 補上使用者提供 CSV 格式的回歸測試，覆蓋 `悠優 | 智慧手機 | 商品序號 | IMEI` 情境
- [x] 重新以使用者提供的 CSV 驗證上傳後按下開始匯入可正常建立資料
- [x] 刪除指定採購單 `PO-20260422-21` 的匯入商品資料與關聯待點貨任務
- [x] 將匯入作業的商品類別顯示從 `智慧手機 / Android` 類型改為僅顯示商品類別，不再把品牌混在同一欄
- [x] 將設定中的品類結構調整為 `商品類別 | 品牌` 兩欄分離
- [x] 在管理後台的品類設定頁支援新增與刪除品類設定
- [x] 清空目前所有既有品類設定資料
- [x] 在 CSV 匯入格式、匯入頁主表與匯入後資料中新增品牌欄位
- [x] 補上品類結構重整、品牌欄位匯入與管理後台品類增刪的回歸測試
- [x] 重新驗證刪除 `PO-20260422-21`、品類清空後重建，以及含品牌欄位的 CSV 匯入流程
- [x] 在管理後台實際新增一筆「商品類別＋品牌」設定並刪除該設定，驗證前端操作、後端資料變更與刷新後結果一致
- [x] 在清空後先於管理後台重建至少一筆品類設定，再以含品牌欄位的 CSV 實際匯入一次，驗證匯入後資料與摘要會顯示品牌
- [x] 補充品牌欄位的端到端驗證紀錄，確認匯入後資料、站點/採購單摘要與同步輸出格式皆包含品牌
- [x] 使用者已確認採用 A 方案：清空品類設定後，既有匯入資料與 A1 待點貨摘要維持顯示為「未分類／未設定」，不保留歷史商品類別與品牌文字
- [x] 刪除品牌欄位驗證用採購單 `PO-20260422-32` 的匯入商品資料與關聯待點貨任務
- [x] 核對 `PO-20260422-32` 在刪除前的商品筆數與關聯任務筆數
- [x] 驗證 `PO-20260422-32` 刪除後，不再出現在匯入頁待點貨採購單摘要與 A1 待點貨資料中
- [x] 重寫匯入頁流程為僅保留「上傳 CSV → 按開始匯入」，移除目前舊的匯入實作邏輯
- [x] 將匯入欄位與匯入條件統一為 `廠商｜商品分類｜商品批號｜商品序號｜IMEI｜品名`
- [x] 從匯入頁、匯入寫入邏輯與摘要顯示中完整移除品牌欄位
- [x] 讓商品分類直接保存 CSV 原始文字，不再依賴後台品類設定或分類對應表
- [x] 因匯入契約改為 `categoryName` 純文字，已同步調整 A1 相關表單與任務顯示，確保新版匯入資料可在站點流程中正確延續
- [x] 以使用者提供格式的 CSV 重新完成端到端驗證，確認上傳後可直接開始匯入且不再被其他資料依賴阻擋
- [x] 重寫 products 匯入資料模型：新增 importedCategoryName 欄位並移除匯入對 categoryId 的依賴
- [x] 重寫 station.importBatch / station.receive 匯入契約，改為接收 categoryName 純文字
- [x] 重寫匯入頁與前後端匯入工具，移除品牌與品類設定依賴，只保留 CSV 上傳後直接匯入
- [x] 更新新版無品牌匯入的前後端回歸測試並完成實際 CSV 驗證
- [x] 修復 /import 頁面 API Query 回傳 HTML 導致的 JSON 解析錯誤，確認所有匯入頁查詢都能正常回傳 tRPC JSON
- [x] 完成 /import 頁面重啟後的實際重載驗證，確認頁面查詢恢復正常且不再出現 HTML 被當成 JSON 解析的錯誤
- [x] 重做 A1 點到貨新增／補齊 UI，移除 PO 單號、廠商、到貨時間、商品分類欄位，改為掃描槍優先的快速輸入流程
- [x] 實作 A1 以商品批號／商品序號／IMEI 快速比對已匯入資料，補齊缺漏欄位後直接完成 A1 並跳轉 A2
- [x] 優化 A1 掃碼體驗與回應速度，讓掃描槍可連續快速刷入
- [x] 在 A1 完成時回寫指定 Google Sheet「採購單」工作表 A:H 欄，包含點到貨時間
- [x] 補上 A1 掃碼補齊與 Google Sheet 回寫的回歸測試與實際驗證
- [x] 以瀏覽器實際完成一次 A1 掃碼提交，驗證命中既有匯入資料後會自動完成 A1 並跳轉 A2，並保留驗證紀錄
- [x] 補強 A1 掃描槍 UX：提交後自動清空欄位並回焦、支援 Enter 快速送出，並以測試或實際操作證明可連續刷入
- [x] 修正 Google Sheet 回寫流程，確保 A1 completedAt 實際寫入「採購單」H 欄，並以讀回工作表資料證明時間欄非空
- [x] 補充端到端驗證紀錄，涵蓋 A1 掃碼成功、A2 跳轉，以及對應列的 Sheet 點到貨時間已更新
- [x] 以新的 A1 完成案例驗證背景自動同步：不手動執行 sync 指令，直接讀回 Google Sheet 對應列並確認 H 欄完成時間已更新
- [x] 以可重現方式驗證 A1 提交後的清空與回焦行為，若成功後立即跳轉則以測試覆蓋該 UX 邏輯
- [x] 新增 A1 測試情境：只有 IMEI、沒有序號與商品批號時，驗證輸入與資料回寫結果
- [x] 新增 A1 測試情境：只有序號、沒有 IMEI 與商品批號時，驗證輸入與資料回寫結果
- [x] 新增 A1 測試情境：只有商品批號、沒有序號與 IMEI 時，驗證輸入與資料回寫結果
- [x] 補回 A1 的下拉式品名欄位，讓現場可在掃碼補齊時選擇或確認品名
- [x] 修正 Google Sheet 回寫，確保品名寫入「採購單」工作表 G 欄
- [x] 將 A1 完成後的行為改為留在 A1 等待下一筆輸入，不再自動跳轉 A2
- [x] 修正 A1 補齊後的 Google Sheet G 欄品名仍為空白問題，確認當 A1 掃碼帶入品名時，products.productName 會更新，且同步後「採購單」工作表 G 欄會寫入實際品名
- [x] 將 A1 回寫 Google Sheet 的日期時間格式統一改為 YYYY/MM/DD HH:mm，並確認 Google Sheet 實際顯示為固定文字格式
- [x] 確認並優化 A1 掃碼比對流程完全使用本地資料庫，改為單次查詢比對 IMEI／序號／批號優先順序，避免等待過久
- [x] 找出 A1 按完成後準備下一筆時比對仍然過慢的瓶頸，改為先清空欄位與回焦、用快取移除已完成項目，並改成背景刷新其他查詢以縮短等待時間
- [x] 釐清 A1 完成後下一筆仍偏慢不是由 Google Sheet 回寫阻塞，並已改成主流程只保留本地 DB 完成，Google／事件／A2 任務與 A1 任務收尾改為背景同步
- [x] 為 A1 完成流程加入主流程與背景同步分離的效能驗證，確認可在本地完成後立即輸入下一筆，最新 station.receive 約 0.97 秒
- [x] 修正並重新跑完整測試套件至全綠後，再交付 A1 主流程背景化優化，最新為 54/54 通過
- [x] 補上回歸測試：驗證 A1 完成後背景建立 A2 任務、背景完成 A1 任務，且 station.detail 不會顯示已離站商品
- [x] 補上可重現的效能驗證紀錄，證明 A1 主流程回應不等待 Google／背景副作用且穩定低延遲
- [x] 將 A1 品名欄位從下拉式選單改為可模糊搜尋的輸入式選單，提升長品名清單的輸入效率
- [x] 補上 A1 品名模糊搜尋的回歸測試，確認掃碼與送出流程不受影響
- [x] 以可重現回歸驗證 A1 品名模糊搜尋：輸入關鍵字、選取品名、確認送出 payload 會帶入 productName
- [x] 補上更貼近流程的回歸驗證，確認 A1 模糊搜尋選品後不影響掃碼、送出、清空與回焦流程
- [x] 將 A2 安裝改為刷入商品批號 QR 後立即完成本站並推進下一站，主流程只寫本地 DB 後立刻返回
- [x] 將 A2 完成時間改為背景回寫到指定 Google Sheet 的 I 欄，格式固定為 YYYY/MM/DD HH:mm
- [x] 補上 A2 掃碼即時完成與 Google Sheet I 欄回寫的回歸測試與互動驗證
- [x] 驗證 A2 完成 API 的實際耗時與主流程步驟，提供可重現證據證明主流程僅做本地 DB 寫入後即返回，最新 station.complete 約 1.91 秒
- [x] 修正並驗證 A2 完成後的自動背景採購單同步，確認補上背景觸發後不需手動執行 sync-purchase-sheet.mjs 也會將完成時間寫入 Google Sheet I 欄
- [x] 補上 A2 端到端驗證：完成 A2 後等待背景同步，自動讀回 Google Sheet I 欄確認時間已更新且格式為 YYYY/MM/DD HH:mm
- [x] 在 A2 站點掃碼完成後自動播放提示音，提供現場即時成功回饋
- [x] 確認 A2 掃碼成功後會立即重新聚焦到輸入框，且不影響快速完工與背景同步流程
- [x] 將 B 站改為搜尋批號完成軟體測試後立即推進下一站並回到可輸入狀態，主流程只寫本地 DB 後立刻返回
- [x] 將 B 站完成時間改為自動背景回寫到 Google Sheet J 欄，格式固定為 YYYY/MM/DD HH:mm
- [x] 在 B 站新增電池檢測對話框，支援手動輸入數字或符號，並新增三個可勾選項目：電池膨脹、副廠電池、電池異常
- [x] 將 B 站電池檢測結果自動背景回寫到 Google Sheet K 欄，將手動輸入內容與勾選項目合併為同一格，格式如「88, 電池異常」
- [x] 將 B 站故障狀態自動背景回寫到 Google Sheet L 欄，將所有勾選項目合併為同一格；若未填寫則回寫「正常」
- [x] 補上 B 站快速完成、電池檢測與 Google Sheet J/K/L 欄回寫的回歸測試與互動驗證
- [x] 續作並整合交付 B 站改版：完成快速推進下一站、電池檢測輸入與 Google Sheet J/K/L 欄自動背景回寫
- [x] 調查並修復批號 00500012121 在 B 站完成後資訊未正確同步到 Google Sheet 的問題，釐清資料庫狀態、sheetSyncJobs、列比對與背景回寫流程
- [x] 將 B 站已選的故障狀態與電池檢測結果自動帶入 C 站表單，且 C 站可修改後再送出
- [x] 將 C 站「故障項目」文案與語意改為「螢幕狀態」，沿用可勾選項目但配合新名稱與資料流
- [x] 將 C 站「外觀項目」文案與語意改為「機身外觀」，沿用可勾選項目但配合新名稱與資料流
- [x] 將 C 站改為搜尋批號後完成測試立即推進下一站並回到可輸入狀態，主流程只寫本地 DB 後立刻返回
- [x] 補上 C 站完成後的自動背景同步觸發，不需手動執行同步腳本，將完成時間寫入 Google Sheet M 欄，格式固定為 YYYY/MM/DD HH:mm
- [x] 將 C 站螢幕狀態勾選結果自動背景回寫到 Google Sheet O 欄，所有勾選內容合併在同一格；若未填寫則回寫「正常」
- [x] 補上 C 站承接 B 站資料、快速完成與 Google Sheet M/O 欄回寫的回歸測試與互動驗證
- [x] 在 C 站表單中承接 B 站的故障狀態與電池檢測結果，且允許 C 站直接修改這些 B 站資料
- [x] 將 C 站「故障項目」正式改名為「螢幕狀態」，並調整相關文案、欄位語意與送出資料結構
- [x] 將 C 站「外觀項目」正式改名為「機身外觀」，並調整相關文案、欄位語意與送出資料結構
- [x] 將 C 站改為搜尋批號完成測試後立即推進下一站並回到可輸入狀態，主流程只寫本地 DB 後立刻返回
- [x] 補上 C 站完成後的自動背景同步觸發，不需手動執行同步腳本，將完成時間寫入 Google Sheet M 欄，格式固定為 YYYY/MM/DD HH:mm
- [x] 將 C 站螢幕狀態勾選結果自動背景回寫到 Google Sheet O 欄，所有勾選內容合併在同一格；若未填寫則回寫「正常」
- [x] 將 C 站機身外觀勾選結果與螢幕狀態合併回寫到 Google Sheet O 欄，同一格以逗號串接；若兩者皆未填寫則回寫「正常」
- [x] 已記錄：舊版 C 站欄位規格（修改上一站時回寫 N 與 K/L）已由新版 H:S 映射需求取代，現行規則改為回寫 M/N 與 Q
- [x] 已記錄：舊版 C 站驗證需求（M/N/O/K/L）已由新版 H:S 映射驗證需求接手，不再以舊欄位作為交付基準
- [x] 已記錄：舊版 C 站 M/O 欄自動同步驗證需求已被新版 P/Q/R/S 欄規格取代，後續實際驗證以新版欄位映射為準
- [x] 已記錄：舊版 C 站 O 欄合併摘要驗證需求已被新版 R/S 分欄規格取代，現行驗證改為分別確認螢幕狀態與機身狀態
- [x] 已記錄：舊版 C 站自動背景同步成功路徑需求已併入新版 H:S 欄位映射驗證清單
- [x] 已記錄：舊版 C 站 O 欄正常值驗證需求已被新版 R/S 分欄規格取代；現行案例改驗證 R/S 於空白情境下回寫「正常」
- [x] 依最新規格修正 A1 站 Google Sheet 欄位：H=點到貨時間、I=A1 執行人名稱
- [x] 依最新規格修正 A2 站 Google Sheet 欄位：J=安裝軟體時間、K=A2 執行人名稱
- [x] 依最新規格修正 B 站 Google Sheet 欄位：L=軟體測試時間、M=電池檢測、N=B 站故障狀態、O=B 站執行人名稱
- [x] 依最新規格修正 C 站 Google Sheet 欄位：P=測試時間、Q=是否修改 B 站狀態回覆（Y/N）、R=螢幕狀態、S=機身狀態
- [x] 重新核對並保留未要求變更的資料流程，只調整欄位映射與必要回寫邏輯
- [x] 補上 Google Sheet H 到 S 欄最新對應的回歸測試與實際同步驗證
- [x] 針對 A1/A2/B 各補一筆實際 Google Sheet 驗證，確認執行人名稱分別正確寫入 I/K/O 欄且來源為帳號管理名稱
- [x] 以至少一筆含 B 站資料的案例驗證 Google Sheet L/M/N/O 四欄值與最新欄位對應完全一致
- [x] 以兩筆 C 站案例分別驗證：一筆 Q=N、R/S=正常；另一筆有修改上一站資料時 Q=Y，且 R/S 正確回寫
- [x] 補齊 H 到 S 欄位映射的單元／整合測試斷言，明確覆蓋每個新欄位對應與 row 輸出順序
- [x] 將 C 站中的 B 站故障狀態改為先以文字結果顯示，只有按下修改按鈕後才展開為可編輯的選項列表
- [x] 將 C 站中的電池檢測改為先以文字結果顯示，只有按下編輯電池檢測後才進入可修改狀態
- [x] 調整 C 站送出前的差異判定，只在使用者實際進入編輯並修改 B 站資料後才視為上一站資料異動
- [x] 補上 C 站文字帶入與編輯模式切換的最小來源碼測試，先鎖住文字帶入與修改入口
- [x] 將 C 站的 B 站故障狀態改為預設顯示文字結果，只有按下「修改 B 站狀態」後才展開可編輯選項
- [x] 將 C 站的電池檢測改為預設顯示文字結果，只有按下修改按鈕後才進入可編輯狀態
- [x] 依最新開發策略停止全量回歸測試與 Google Sheet 手動驗證，本輪聚焦 C 站文字帶入與編輯模式切換的最小改動
- [x] 本輪只補最小範圍的 C 站 UI 單元測試，避免再跑整個站點流程
- [x] 在邏輯確認前暫不做完整 Type Refactoring，只需確保最終 TypeScript 編譯通過
- [x] 依最新優先順序直接聚焦修改 StationPage.tsx 的 C 站介面，暫停其他廣泛收尾工作
- [x] 將 C 站的 B 站故障狀態固定為預設純文字顯示，只有按下修改按鈕後才展開編輯模式
- [x] 整理目前 StationPage.tsx 的 React Code 片段，直接交給使用者檢視
- [x] 本輪僅執行與 C 站改動直接相關的最小測試：server/c-station-battery-interaction.test.ts 與 server/c-station-text-mode.test.ts
- [x] 優先直接交付 StationPage.tsx 的 React Code，再視需要補測試與其他收尾
- [x] 直接修改 C 站品檢介面，讓 B 站故障狀態先以文字結果帶入，只有按下修改按鈕時才進入可編輯狀態
- [x] 直接修改 C 站品檢介面，讓電池檢測先以文字結果帶入，只有按下修改按鈕時才進入可編輯狀態
- [x] 修正 C 站 batterySummary fallback，避免 inheritedBatterySummary 與已初始化的 batteryNote 或 batteryIssueLabels 同時出現造成重複摘要
- [x] 更新或新增 C 站測試，驗證電池檢測在未進入編輯前只顯示單一正確文字摘要，且按下修改後才進入可編輯流程
- [x] 補上一個可執行的 C 站互動測試，實際驗證電池檢測預設只顯示摘要、點擊修改後才開啟可編輯 UI
- [x] 在 C 站作業頁新增「鏡頭狀態」區塊，支援勾選多個鏡頭狀態並納入送出流程
- [x] 在管理後台功能表設定新增「C 站鏡頭狀態」設定區塊，支援新增項目、排序與啟用開關
- [x] 讓 C 站鏡頭狀態選項只顯示已啟用項目，並可由管理後台新增後立即生效
- [x] 補上 C 站完成後的背景同步，將鏡頭狀態摘要回寫到 Google Sheet 指定列 T 欄；若未填寫則回寫「正常」
- [x] 依使用者最新要求暫停主動執行小規模測試，後續改以功能實作與程式碼整理為主，等待使用者手動驗證
- [ ] 由使用者手動驗證 C 站鏡頭狀態送出流程，確認完成時會帶出 cameraOptionIds 並正常推進下一站
- [ ] 由使用者手動驗證管理後台新增或啟用的 C 站鏡頭狀態項目，確認 C 站頁面重新整理後可立即顯示
- [ ] 由使用者手動驗證 Google Sheet 採購單 T/U/V 欄同步，確認 T 欄為 C 站測試人員、U 欄為完成時間、V 欄為鏡頭狀態；未勾選鏡頭時回寫「正常」
- [x] 將 Google Sheet 採購單同步改為 C 站完成後自動寫入 T 欄測試人員名稱
- [x] 將 Google Sheet 採購單同步改為 C 站完成後自動寫入 U 欄完成時間，格式為 YYYY/MM/DD HH:mm
- [x] 重新整理採購單同步欄位映射，確認 C 站鏡頭狀態與新加入的 T/U 欄位不互相衝突
- [x] 排查商品序號 T4FF2XJF60 為何未寫入 Google Sheet，確認根因是採購單背景同步腳本在近期欄位調整後同時出現 JavaScript 語法錯誤與 SQL 欄位名稱不一致，導致自動同步未執行
- [x] 修正商品序號 T4FF2XJF60 的 Google Sheet 寫入問題，已修復同步腳本並手動補跑一次採購單同步，讓資料重新回寫
- [x] 已直接核對 Google Sheet 採購單第 4 列，確認商品序號 T4FF2XJF60 目前寫入為 T=綠途未來股份有限公司、U=2026/04/24 03:37、V=破裂
- [ ] 將 E 站改為支援刷碼／條碼輸入，確認完成抹除後可按完成並推進下一站
- [ ] 讓 E 站完成後自動回到刷碼／條碼輸入狀態，以利下一台設備連續作業
- [ ] 將 E 站抹除完成背景同步回寫到 Google Sheet，Z 欄為完成時間、AA 欄為測試人員
- [ ] 排查商品序號 YLXGHQXR36 的 E 站完成時間為何在 Google Sheet Z 欄寫成 2026/04/24 05:12
- [ ] 修正 E 站完成時間的 Google Sheet 時區或格式轉換問題，確認 Z 欄顯示為正確的現場時間
- [ ] 統一修正 A1、A2、B、C、D、E 站完成時間來源，改為以應用端實際時間為準
- [ ] 排查並校正各站 Google Sheet 完成時間欄位，避免資料庫時鐘快 4 小時造成寫入時間錯誤
- [ ] 排查目前無法登入的問題，確認是 OAuth、Cookie、回呼網址或服務設定哪一段失敗
- [ ] 修正登入流程，讓使用者可重新正常登入 quality-dashboard-mvp
- [ ] 排查登入時出現 403 與 OAuth callback failed 的原因，確認是回呼網址、授權設定、Cookie 或站內流程哪一段失敗
- [ ] 修正登入流程，讓 quality-dashboard-mvp 可正常完成 OAuth callback 並建立登入狀態
